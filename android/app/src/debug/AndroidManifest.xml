<?xml version="1.0" encoding="utf-8"?>
<!--
  Debug-only manifest overlay. Allows cleartext HTTP so the app can talk to a
  local backend (http://10.0.2.2:4000 from an emulator, or http://<lan-ip>:4000
  from a physical device on the same Wi-Fi). Does NOT apply to release builds.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <application
        android:usesCleartextTraffic="true"
        tools:replace="android:usesCleartextTraffic"
        tools:targetApi="28" />
</manifest>
